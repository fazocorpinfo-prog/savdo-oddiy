[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback — barcha yo'llar index.html ga (API'dan keyin)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
